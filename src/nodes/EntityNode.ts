import { Node } from "./Node";
import { AngleNode, ChoiceNode, ColorNode, InputNode, MaterialNode, ModelNode, NumberNode, ToggleNode, VectorNode } from "./BasicNode";

export default class EntityNode extends Node {
	/*private model: ModelNode;
	private material: MaterialNode;
	private bone: InputNode;
	private boneMerge: ToggleNode;
	private position: VectorNode;
	private angles: AngleNode;
	private eyeAngles: ToggleNode;
	private size: NumberNode;
	private scale: VectorNode;
	private positionOffset: VectorNode;
	private angleOffset: AngleNode;
	private color: ColorNode;
	private brightness: NumberNode;
	private alpha: NumberNode;
	private fullbright: ToggleNode;
	private cellShade: NumberNode;
	private translucent: ToggleNode;
	private invertRendering: ToggleNode;
	private doubleFaceRendering: ToggleNode;
	private skin: NumberNode;
	private lodOverride: NumberNode;
	private passes: NumberNode;
	private tintColor: VectorNode;
	private lightBlend: NumberNode;
	private modelFallback: ModelNode;
	private textureFilter: NumberNode;
	private blurLength: NumberNode;
	private blurSpacing: NumberNode;
	private usePlayerColor: ToggleNode;
	private useWeaponColor: ToggleNode;
	private ignoreZ: ToggleNode;
	private noTextureFiltering: ToggleNode;
	private blendMode: ChoiceNode;
	private drawOrder: NumberNode;*/
}